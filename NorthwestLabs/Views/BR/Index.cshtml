@model IEnumerable<NorthwestLabs.Models.Tests>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Test_Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Test_Details)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Test_Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Test_Details)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Test_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Test_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Test_ID })
        </td>
    </tr>
}

</table>
