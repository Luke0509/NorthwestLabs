@model IEnumerable<NorthwestLabs.Models.Assay>

@{
    ViewBag.Title = "Protocol";
    Layout = "~/Views/Shared/_SeattleEmpLayout.cshtml";
}

<h2>Protocol Notebook</h2>

<p>
    @Html.ActionLink("Create New", "CreateAssay")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Assay_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Assay_Summary)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Assay_Details)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Assay_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Assay_Summary)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Assay_Details)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditAssay", new { id = item.Assay_ID })
            @Html.ActionLink("Delete", "DeleteAssay", new { id = item.Assay_ID })
        </td>
    </tr>
}

</table>
<a class="btn btn-outline-danger my-2 my-sm-0" href="@Url.Action("ChooseCatalog", "SeattleEmployee")" style="font-size: 14px">Go Back</a>